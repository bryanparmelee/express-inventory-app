extends layout 

block content       
    h1= title
    //- img.section-img(src='/images/gear.jpg')
    div.items-container
        each item in item_list 
            a.item-container(href=item.url)
                img(crossorigin="anonymous" src=item.image alt=item.name) 
                p.item-title #{item.name}

        else
            p There are no items 